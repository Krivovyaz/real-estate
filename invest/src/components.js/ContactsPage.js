import React from 'react'
import styled from 'styled-components';

function ContactsPage({setShowFooter}) {
    setShowFooter(true);
    return (
        <Container>
            
        </Container>
    )
}

export default ContactsPage

const Container = styled.div``
